import StoryClient from "./StoryClient";

const StoryPage = () => {
  return (
    <div className="flex m-0 p-0 max-h-full h-full w-full max-w-full flex-col gap-10 bg-black">
      <StoryClient />
    </div>
  );
};

export default StoryPage;
